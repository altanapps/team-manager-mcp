{
  "name": "team-manager-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MongoDB-native MCP team manager for multi-agent collaboration.",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "atlas:init": "tsx scripts/init-atlas.ts",
    "atlas:smoke": "tsx scripts/smoke-filtered-vector.ts",
    "mcp": "tsx scripts/mcp-server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "mongodb": "^6.12.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.9"
  }
}
